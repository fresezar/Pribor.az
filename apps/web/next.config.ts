import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Faz 1'de: i18n (az/ru/en) app-router segment stratejisiyle, ISR ile semt sayfaları
};

export default nextConfig;
